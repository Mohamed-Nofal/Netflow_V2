function f(x, def) {
    if (!def)
        return x;
    else
        return x;
}

function ReLu(x) {

    return Math.max(0, x);
}

function Sigmoid(x) {

    return 1 / (1 + Math.pow(2.718282, -x));
}